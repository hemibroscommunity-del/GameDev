/* Shared cache-bust version for the layered gear/armor sprite sheets under
 * public/sprites/gear/<slot>/<item>/<pose>-<dir>.png.  Bump this whenever a
 * gear PNG changes so clients refetch it.  Lives in its own leaf module so
 * both the renderer (effectsRenderer) and the preloader (combatGear) can share
 * the exact same string without importing each other (avoids an import cycle)
 * and without the two URLs drifting out of sync (which would double-fetch). */
export const GEARLAYER_VER = '1032';   /* 1032 (v2.3.1465): chop greaves replaced with owner-regenerated hand-look art (chain waist band, plated legs, steel sabatons; fit via tools/fit_chop_legs_art.py — constant scale, feet-pinned, v1463 chain kept as the waist seal); 1031 (v2.3.1463): chop finish — crotch/waist sash chain-filled with the stand sheet's real chain-belt weave (owner: "crotch area textured with the gray chain belt like jogging"), boots remapped onto the ramp's bright segment so they read as plate sabatons, pants fringe swept; 1030 (v2.3.1458): chop greaves extended over shins + boots — the art hard-stopped at 0.80x frame height leaving ~43 body rows of bare cloth/boot under a full steel set (owner: "armor doesn't cover shins and boots"); plated from the body strip's own leg pixels via tools/plate_chop_shins.py so coverage tracks every swing frame; 1029: chop chest plate +10px on frames 3,4; 1028: chop shirt +5px more on frame 4; 1027: chop shirt +5px more on frames 3,4; 1026: chop shirt +5px more on frames 3,4,7,8,12; 1025: chop shirt nudged +10px on frames 2,3,4,7,8,11,12 to sit on the torso; 1024: woodcutting chop-west gear layers (chest/legs/shirt); chop shirt is a grayscale tintable base so it recolours to the player's shirt colour; 1023: add shirt swing-north.png (serves north + northeast + mirrored northwest) -- tinted shirt now shows on north sword swings too, neck/waist-anchored; 1022: add shirt swing-east.png (also serves mirrored west); 1021: shirt swing-south uses ONE constant transform for all frames (collar pinned, fixed scale) -> no per-frame bounce/flicker; 1020: collar lock */
