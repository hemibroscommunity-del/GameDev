> **Evidence appendix for `docs/BACKLOG-TRIAGE-2026-09-14.md` (v2.3.2471).** Raw checkpoint log of a read-only research pass against `main` 8f74d42 (v2.3.2470). Line numbers are as of that commit — grep the quoted identifier if they drift. `TODO` lines are the researcher's own open threads at the time of writing, not work items. KNOWN = read in code; INFERRED = the reading the code most directly supports.

# Combat-numbers rescale triage — checkpoint file
(started 2026-09-14; READ-ONLY research; findings appended per sub-question)

## Q1 partial (server side, KNOWN)
- server/src/data.js:28 `MONSTER_HP_CURVE = { base: 12.5, ramp: 1.052, plateau: 1.035, endgame: 1.025, flat: 100, flatLow: 50, flatLowMaxLvl: 2 }` — HP = ceil(monsterStat(12.5,lvl)) + flat(100 | 50 for Lv<=2). Mirrored client gameSystems.js.
- server/src/data.js:48 `DAMAGE_CHANNEL_FLAT = 1` (legacy T2; reads 0 under prog3 via _t2Flat gate — combat.js:604 "dmgFlat above reads 0 for prog3").
- server/src/data.js:131 monster DMG curve inline `monsterStat(12, B, 1.045, 1.025, 1.018)` (base 12) — comment says dmg/xp/gold curves "stay inline at those call sites" (_spawnZoneMonsters index.js, _dungeonMonster dungeon.js).
- server/src/data.js:66-70 T2_UNITS (legacy accelerating flats: damage 1, critDmg 4, ironskin .5 ...) and :145-155 T2_BENCH (percent of benchmark — multiplier-of-monster, moves automatically).
- server/src/data.js:265-272 ARCHETYPES hpMult/dmgMult (multipliers — note only).
- combat.js:398 fists fallback `6.25` flat; combat.js:489 `_maxDmgForAttacker` floor `21` flat and `Math.max(21, ceil((maxWpn*critMult+critFlatCeil)*5*specialMult))`; combat.js:41 CRIT_ANCHOR_MULT=2 (mult).
- combat.js:697-699 VAR bands staff [0.5,1.65], bow [0.6,0.8], melee [0.75,1.25] — "SIX COPIES MOVE TOGETHER: combat.js, client two rolls + displayed range in gameSystems.js, tools/balance-sim.mjs, BAND_TOP in server/test/prog3.test.mjs".
- combat.js:752 `return { dmg: Math.max(1, Math.round(base)), isCrit }` — ROUND at end of roll, floor 1.
- combat.js:903-905 `rawDmg = Math.max(1, Math.round(...)); actualDmg = Math.min(rawDmg, Math.max(0, m.hp))` — clamp to remaining HP (Q4).
- Files referencing DMG_PER_LEVEL/HP_PER_LEVEL/etc: server/src/{burst,combat,cooking,data,dungeon,firetrail,grids,index,prog3,quests,telegraph}.js; tests blockcost,burrow,combat-lifecycle,display-dps,dungeon,mirror-audit,potions,prog3,tick; client src/data/{gameSystems,prog3}.js, src/game/playerActions.js, src/networking/wsClient.js, src/ui/BroTown.jsx, src/ui/XpFlyOverlay.jsx; tools/qa/mp/{mp-burst,mp-prog3,mp-townlock}.mjs, tools/verify-prog3-retune.mjs.
## Q1 cont. (KNOWN unless marked)
- Weapon bases (AUTHORITATIVE server): server/src/gear.js:67 `const T = { greatsword: 10, sword: 6.67, bow: 12.80, staff: 13.44 }`, fists fallback 6.25 (:82). NOTE: bow/staff were re-based v2.3.2259 (BALANCE-PLAN §1 table 7.29/8.54 is STALE). Client mirror src/data/gameSystems.js:2611 WEAPON_TYPES base 10/6.67/12.80/13.44. combat.js:398 duplicates fists 6.25.
- Hardness: hardening.js HARDEN.BASE_BONUS (client 1.0417 gameSystems.js:4970) — flat add to base per hardness level → MUST move.
- prog3 (server/src/prog3.js:209-211 / client src/data/prog3.js:62-63): DMG_PER_LEVEL {sword 1.5, bow 1.5, staff 1.8}, HP_PER_LEVEL 6; ATK.dmg per 0.5 (cap 75) FLAT pre-tier (prog3.js:171); BODY.hp per 8 (cap 100) FLAT maxHp; BODY.elem per 1 (feeds elemental legacy-power formulas); BODY.stam per 3; MANA_PER_MAGIC_LEVEL 2.5, SPECIAL_MANA_COST 25, BURST_MANA_COST 25 (mana axis); BURST_DMG_MULT 1.5 (mult); BURST_RADIUS 70 (px).
- Player maxHp: prog3 `ps.maxHp = floor(100 + level*6 + hp*8)` (server prog3.js:565); legacy calcMaxHp `floor(100 + (level-1)*2.5 + vitality*10)` (gameSystems.js:4833, HP_PER_COMBAT_LEVEL 2.5) mirrored by grids.js:577 _recomputeMaxes. Base 100 is a flat.
- Monster attack dmg: `baseDmg = _monsterStat(12, lvl, 1.045, 1.025, 1.018)`; `dmg: ceil(baseDmg*a.dmgMult)` — index.js:1377/1417, dungeon.js:304/320, data.js:131 (t2BenchStats). XP `monsterStat(10,..)`, gold `monsterStat(5,..)` (NOT combat — but XP_PER_DMG 0.4 pays XP per damage point → rescaling damage rescales XP rate unless XP_PER_DMG moves inversely; prog3.js:52 comment: "the two dials are one system").
- Telegraph (server/src/telegraph.js:87-105): MAX_HIT_PCT 0.5 (pct), kits dmgMult 2.0/1.5/1.0 (mult), radius px; SLIME_BURST.DMG = 60 FLAT (telegraph.js:283) → must move; execute `min(ceil(kit.flat)|ceil(m.dmg*dmgMult), floor(maxHp*0.5))` (:448-451).
- Dungeon (dungeon.js): boss `m.dmg = ceil(m.dmg*1.5)` (:371), bossMultiplier/scale, SUMMON.HP_MULT, ENRAGE pct, SIPHON.HEAL_PCT — all multipliers/pcts; MAX_HIT_PCT 0.5. dungeon.js:430 block cost literal `15 * _blockStaminaMult` (stamina; vs BLOCK_STAMINA_COST=10 data.js:1031 — drift, stamina axis).
- Abilities (server/src/abilities.js): dmgMult 1.0 (whirlwind), 0.75 (bash), lunge 0.6 — multipliers of the normal roll; costs are % of maxStamina (:68 "costs were already percentages"); stunMs/radius px.
- Burst (burst.js:198-208): `round(rolled.dmg * 1.5)` clamped to _maxDmgForAttacker → mult.
- Elemental (server/src/elemental.js): FLAT DoT `burn 5 + P*0.3`, `root 3 + P*0.15` (:172-173), thorn 4+P*0.25 (comment :91), COLLISION_TABLE base 20..120 + stat*coeff (:29) — ~45 flat bases + 2-3 DoT flats → MUST move; COLLISION_BURST_CAP 3.2 (mult); FRACTURE +6%/stack (pct). Client mirror in gameSystems.js (tables "extracted programmatically").
- Heals: cooking.js:47 `_fishHealAmount = ceil(92 + tier.lvl*8)` FLAT; data.js:423 shop `cookedMinnow power 23` (healFish); COOKING_RECIPES buffs regen power 0.02 (pct), resist 0.05 (pct) — combat.js:240 hardcodes `1-0.05`.
- Block stamina: data.js:1025 BLOCK_COSTS_STAMINA true, :1031 BLOCK_STAMINA_COST 10 (per blocked hit), hold drain ~2/s (COMBAT-OVERHAUL PR4). Stamina axis is independent of damage — does NOT need to move for a damage rescale (KNOWN: cost is flat per hit, not per damage).
- Anticheat: _maxWeaponDmg fists 6.25 flat, formula mirrors roll (lockstep by construction for DMG_PER_LEVEL/ATK.dmg/base); _maxDmgForAttacker floor 21 FLAT (combat.js:489,527) + comboBoost 5, specialMult 3.0, critMult ceiling 1.5+critDmg → floor 21 must move; cadence floor 210ms unaffected.
- spawnscale.js SPAWN_SCALE: population/timing only (MON_PER_PLAYER, RESPAWN) — no damage/HP numbers → does NOT move.
- T2 legacy: T2_UNITS (data.js:66) flats, T2_BENCH (pct of benchmark; auto-scales), t2Accel/t2PointValue; banked ps.t2Flat values ALREADY STORED in blobs (v2.3.1451 migration v9) — a rescale would need a migration to shrink stored flats (INFERRED).
- Gladiator/arena/duel: no flat damage/HP numbers (only hp/maxHp fraction tiebreaks) — KNOWN from grep.
## Q1 final bits (KNOWN)
- Regen (index.js:2983-3126): hub HP `ceil(maxHp*0.10)`/tick, spoke OOC `round(maxHp*0.01)` (SPOKE_REGEN_PCT), mana 0.018/0.007 of maxMana — all PERCENT of pools → do not move (but `Math.max(1, ...)` floors matter at small HP).
- Monster basic attack → player: index.js:1523 `this._applyDamage(targetPs, m.dmg, _blocking)`; _applyDamage rounds `r = max(1, round(rawDmg))` (combat.js:181) and floors dmgTaken at 1 after every mult (lines 240,249,270,279,289).
- PvP (combat.js:1400-1480): `dmgBase = max(1, min(dmgCap, payload.dmgBase || 10))` — client-supplied dmgBase clamped by _maxDmgForAttacker; flat default 10; `PVP_TUNING.RANGE_CAP` px. PvP hit payload hp `round(targetPs.hp)` (:1637).
- Tests/tools that PIN numbers: server/test/mirror-audit.test.mjs (§1 ARCHETYPES, §2 MONSTER_HP_CURVE exact, DAMAGE_CHANNEL_FLAT, T2_UNITS, T2_BENCH(+CANONICAL), §12 PROG3 scalar flatten server<->client incl. DMG_PER_LEVEL/HP_PER_LEVEL/BODY/ATK, TELEGRAPH/SLIME_BURST imports, crit formula probe :759); server/test/prog3.test.mjs (:122 maxHp = 100+3*HP_PER_LEVEL, :233 +8/pt, :371/774/811 preVar uses DMG_PER_LEVEL & _weaponEffBase, :770 `BAND_TOP = { melee: 1.25, ranged: 0.8, staff: 1.65 }`); server/test/anticheat.test.mjs (:136 dmgCap from _maxDmgForAttacker, sell value `coins === 27` :417 — sell value derives from _weaponBase!); server/test/display-dps.test.mjs (hand-computed fixture of calcDisplayDmgRange; calcDisplayHeal = ceil(fishHeal×recovery)); server/test/{blockcost,potions,tick,combat-lifecycle,dungeon,burst,telegraph?,abilities}.test.mjs reference HP_PER_LEVEL/heals; tools/balance-sim.mjs (:104 VARIANCE copy, :133-134 monsterStat(12..) copy, :151 expects sword.base+power*0.1667, :523 HARDNESS_BONUS_PER_LVL); tools/audit-validator.py (GDD-scale copies: greatsword 48, MON_BASE_HP 40, MON_BASE_DMG 8 — already has `--k` rescale mode, lines 112-120 `scaled_constants(k)` "Divide every ABSOLUTE damage/HP constant by k (multipliers untouched)" and :199 prints "max ceil()-rounding drift once HP is small"); tools/verify-prog3-retune.mjs (imports server curves; prints hits-to-kill tables); tools/qa/mp/mp-orbline (pins staff/melee DPS ratio 0.74-0.86 per combat.js:657 comment), mp-prog3, mp-burst.
- Sell value: gear.js `_weaponSellValue` derives from _weaponBase ("coins are NOT rescaled" note at gear.js:54) → rescaling bases changes coin economy unless sell formula compensates. anticheat.test pins coins===27.
## Q2 ROUNDING (KNOWN)
Server: roll `dmg: max(1, round(base))` combat.js:752 (post-variance, post-crit); hit `rawDmg = max(1, round(min(cap, rolled)*fracture))` :903; `actualDmg = min(rawDmg, max(0,m.hp))` :905; m.hp integer by construction (spawn `ceil(baseHp*hpMult)+flat` index.js:1397, minus integer dmg). DoT: elemental.js:174 `round(dot)`; collision :234 `round(dmg)`; _applyMonsterDot index.js:2788 `min(max(0,round(raw)), m.hp)`; thorn `round(4+P*0.25)` index.js:1549. Burst: burst.js:200 `round(rolled*1.5)`, :208 `round(*fracture)`. Player-side: _applyDamage `r=max(1,round(raw))` combat.js:181 then floor-1 after every mult (:240 ceil, :249,:270,:279,:289 round); telegraph `min(ceil(m.dmg*mult), floor(maxHp*0.5))` telegraph.js:448-451; monster dmg `ceil(baseDmg*dmgMult)` index.js:1417. Heals ceil (cooking.js:73,:47; lifesteal ceil(taken*0.9) combat.js:374; hub regen ceil(maxHp*0.10) index.js:3042). Display range `round(min/max)` gameSystems.js:5089.
Client: popup prints `'-' + payload.dmg` verbatim (gameEvents.js ~1651/1713/1718) — no client rounding; local melee popup GATED OFF in server zones since v2.3.2220 (monsterCombat.js:2236-2264 `if (!S._serverMonsters)`); curHp = `round(hpPct*maxHp)` gameEvents.js:1556; HP bar frac curHp/maxHp entityRenderer.js:7578-7580; HP text `ceil(curHp)` :7766. Legacy non-server paths use ceil(dmgTaken) (gameEvents.js:2192-2243, pvp_hit :2724-2741 client re-derives ×0.25 block/×1.5 crit — display only).
Consequence: at bow-low=1 the bow band 0.6-0.8 × base rounds to a single integer (all rolls identical), crit anchor `max(dmg*1.5.., rangeTop*2)` → 2; the `max(1,…)` floors dominate. audit-validator.py:199 already warns "max ceil()-rounding drift once HP is small".

## Q3 ATTRIBUTES (KNOWN)
Grid = PROG3 (server/src/prog3.js:44-215, client src/data/prog3.js:24-80): BODY global {def .004, hp 8, dodge .004, stam 3, elem 1(cap 75)}; ATK per combat type {crit .004+base .01, critDmg .01, aspd .0035, dmg .5}. Docs: docs/specs/progression-v3.md (build-skill-progression.md / character-build.md describe the RETIRED 6×5 T2 grid). Elemental damage = BODY.elem GLOBAL (prog3.js:130-139 "BODY (not ATK) per the owner's own split"), consumed via elemAttackStat(ps) (elemental.js:67) at combat.js:116,:954, burst.js:214, elemental.js:217 — no weapon/cat argument. Elemental RESISTANCE attribute: NONE in PROG3; only cooking 'resist' buff 5% all-damage (combat.js:239) and a client-only stone-amulet `elemResist` (items.js:39, monsterCombat.js:779 local-AI path only, not read by server _applyDamage). Max mana: NOT an allocatable stat — `maxMana = floor(100 + magicLvl*2.5)` from the STAFF skill level (prog3.js:580-582); manaBlocks = blocksAt(magicLvl) (abilities.js:497). 
Moving max-mana→global stat / elem→per-weapon touches: PROG3.BODY/ATK both sides (mirror-audit §12 flattens all scalars, mirror-audit.test.mjs:409-440; prog3.test.mjs:125 maxMana derived); prog3FreshAlloc/prog3FreshAtk (prog3.js:296-303); _handleProg3Allocate whitelist via prog3StatDef scope (prog3.js:730-806 — ATK requires `cat`, BODY takes any lane); _prog3Recompute (:562-590) + client recalcDerived (gameSystems.js:5390); elemAttackStat gains a cat param at 4 call sites; HeroExpanded PROG3_BODY_META/ATK_META rows (src/data/prog3.js:100-135, HeroExpanded.jsx:1290-1445). MIGRATION: migrations.js registry `_v` stamps, RPG_SCHEMA_VERSION=14 → next v15 appended (never renumber, :35-42), plus the join boundary heal `_sanitizeProg3` (prog3.js:411, join.js:1003) for fail-open blobs (v11/v14 pattern). Precedent for moving points global→per-type: v11 `prog3-per-type-offense` REFUNDS to pool via prog3SplitAtk (migrations.js:404-431, prog3.js:385-404) — "copying would triple... choosing one type would be guessing". INFERRED: an elem refund can only land in the unchannelled `pool` remainder (blob does not record which lane paid a BODY point).

## Q4 KILL POPUP (KNOWN)
Server clamps: combat.js:905 `actualDmg = Math.min(rawDmg, Math.max(0, m.hp))`; monster_hit payload (:1082-1105) = {monsterId, zone, dmg: actualDmg, isCrit, attackerId, slot, hpPct} — RAW damage NOT on the wire; `rawDmg` is a local (:903). Same clamp in DoT (index.js:2788) and collision (:964 colDmg). Client paints `'-' + payload.dmg` (gameEvents.js:1651/1713/1718). Fix = additive field (e.g. payload.raw) + client prefers it; PRIVILEGED_EVENTS unchanged (existing type). XP/contribution must keep using actualDmg (:915-925 "grinding a corpse can't inflate").

## Q5 MAYOR BRO (KNOWN)
NPCs are CLIENT-LOCAL: NPC_DATA gameDisplay.js:3742 Mayor `hp: 100, maxHp: 100, noHp: true, alive: true`; _spawnTownNpcs BroTown.jsx:700-724 copies into S.npcs; melee swing loop monsterCombat.js:2706-2750 hits any `alive` NPC in SWING_ARC (no noHp/quest-giver gate): `npcDmg = pDmg` (local roll), `npc.hp -= npcDmg`, blood particles, knockback, `pushDmgPopup('' + npcDmg, '#fff')` :2748, KO at hp<=0 with 10s respawn + skull 'npc' (:2750-2777). No server event exists (grep npc_hit/npc_damage: none); projectiles never test S.npcs. Pure local prediction/cosmetic.

## Q6 STAFF AOE (KNOWN)
No splash today: projectile hit loop projectiles.js:608 `(hit && !a.pierce) → return` with `var hit=false` :582 and `a.hit = true` :728 → ONE monster per bolt; staff never pierces (monsterCombat.js:1590 `pierceLeft: isStaff ? undefined : bowPierceCount`). Detonation `staffAoeMult` only widens the single-target hit RADIUS (:672; +1%/pt of legacy weaponSpecs, gameSystems.js:3364 — reads 0 for fresh prog3 chars, INFERRED still >0 for veterans with stored weaponSpecs). WEAPON_TYPES.staff aoeCap/aoeCone (gameSystems.js:2680) are read NOWHERE. Radius nerfed to bow's v2.3.2243. Wire: one `monster_damage {monsterId, zone, element, slot, special}` per target (projectiles.js:787-820); server ignores any client damage number, rolls FULL damage per event (combat.js:893), cadence per (player,monster) 210ms/3-per-1200ms lanes (:868-880), no proximity gate for staff (:840-862). ⇒ 50% splash REQUIRES a server change: either a `splash:true` intent flag (server ×0.5, must stay ≤ dmgCap) or server-side radius scan off the primary hit (burst.js:184-223 pattern). Client-only N events would deal 100% to each.
