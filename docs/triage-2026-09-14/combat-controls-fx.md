> **Evidence appendix for `docs/BACKLOG-TRIAGE-2026-09-14.md` (v2.3.2471).** Raw checkpoint log of a read-only research pass against `main` 8f74d42 (v2.3.2470). Line numbers are as of that commit — grep the quoted identifier if they drift. `TODO` lines are the researcher's own open threads at the time of writing, not work items. KNOWN = read in code; INFERRED = the reading the code most directly supports.

# Combat/controls/FX triage research — checkpoint file (READ-ONLY research, no repo edits)
Repo HEAD 8f74d42 (v2.3.2470). Tags: KNOWN = read in code/spec; INFERRED = reasoning.

## Q1 INPUT — findings so far (checkpoint 1)
- KNOWN: docs/specs/control-redesign.md covers v2.3.2242→2251 only. Later history lives in code comments:
  - v2.3.2258 (BroTown.jsx:8014-8019): "the right control is a JOYSTICK again (owner: 'I want both joysticks back and restore the previous behavior right joystick for auto attack and rotation')" — rKnobRef/rStickRef restored; rJoyAim (BroTown.jsx ~8298) writes S._facing (4-way), S._aimAngle, S._aiming, S.autoAttack from the drag. So the right control = joystick that auto-attacks while deflected/held; NOT a pure button.
  - v2.3.2269 (BroTown.jsx:77-103, RBTN_DBL_MS=300 press-to-press): bow/staff double-tap on right control raises shield (latch). v2.3.2271 keeps lock across the pair (S._rTapLockWas). v2.3.2446 (cf94522): second tap opens a HOLD (S._rShieldHold), release drops via _endShieldHold (BroTown.jsx ~8395), drag steers _shieldAngle raw (not through rJoyAim). v2.3.2451: S._rShieldSteered — hold starts on seed angle (locked target else facing) until thumb drags; rJoyAim returns early when _rShieldHold (no autoAttack/_aiming).
  - v2.3.2465 (BroTown.jsx ~8132): S._atkPressAt — first shot of a HOLD waits so a flick can declare itself (special vs ordinary shot).
- KNOWN: Right-button press handler = handleRBtnPress (BroTown.jsx:8122-8290). Both surfaces (zone rS + disc bS) route through it. Melee: promotes auto lock to src:'tap', sets autoAttack, then maybeSwordDash(S) (proximity lunge). Bow/staff: double-tap branch (above).
- KNOWN: ShieldButton.jsx (src/ui/panels/ShieldButton.jsx): fixed pos right = RBTN.right + (discW-size)/2, bottom = calc(sheet-h + 12px); RBTN = {right:50, bottom:70, w:96, wLand:108}; size 48/54. Centred DIRECTLY BELOW the disc. Tap → toggleShield (shieldToggle.js). Visible per shieldButtonLive(): returns false for activeSlot 'ranged'|'staff' (v2.3.2446), true if _shieldUp / lockedTarget / damage<5s / monster within 220px.
- KNOWN: shieldToggle.js: shieldAimAngle(S) = atan2 to lockAimPoint(lockedTarget.ref) else _shieldAngle else _aimAngle else _facingAngle. raiseShieldToggle clears autoAttack/isSwinging/_aiming, sets _shieldUp/_shieldAngle, broadcasts player_shield, blockRingBus.beginBlock. dropShield(S, why).
- KNOWN: wire: blocking/ba ride every `move` packet (spec §1 table; wsClient). Server resolves ±60° arc at impact (_blockArcCovers). All shield input changes are CLIENT-ONLY; no caps flag needed.

## Q1 checkpoint 2 — surfaces, geometry, double-tap handlers
- KNOWN surfaces (BroTown.jsx ~8935-9130 + ~9130-9330): TWO right-hand touch surfaces since v2.3.2258:
  - ZONE (rZoneRef, right 50% of screen, z6; handlers rS/rM/rE ~8960-9110): press→handleRBtnPress (autoAttack), drag→rJoyAim (aim/facing + autoAttack), release→handleRBtnRelease; flick classifier in rE (last-leg spd>0.15px/ms & dist>8 & dur<400, OR total spd>0.2 & dist>15 & dur<500) → doSpecialAttack(). Short still tap → synthetic canvas click (tap-to-lock).
  - DISC (rJoyRef, class .bt-rjoy-base, pointerEvents auto; handlers bS/bM/bE ~9132-9330): bS → harvest if node in reach, else handleRBtnPress() then doSwing(); bM does NOT steer aim (v2.3.2258 deliberate) but steers _shieldAngle while _rShieldHold, and has SLIDE-DOWN-TO-SHIELD latch (v2.3.2254, reads document.querySelector('[data-shield]') rect → raiseShieldToggle); bE → flick classifier → special.
- KNOWN double-tap handlers:
  - LEFT stick double-tap = weapon cycle: BroTown.jsx lE ~8850-8870: DOUBLE_TAP_WINDOW_MS=220 (release-to-release), TAP_MAX_DURATION_MS=200, 50px; second tap → _desktopCycleWeapon() (nextWeaponSlot in game/weaponSlots.js); first tap forwards canvas click + shows next-weapon preview icon in disc (lJoyPreviewRef, PREVIEW_HOLD_MS 350). VERIFIED: double-tap LEFT stick = weapon swap.
  - RIGHT control double-tap (bow/staff ONLY): handleRBtnPress BroTown.jsx ~8195-8290: RBTN_DBL_MS=300 press-to-press via S._rTapAt; second tap → raiseShieldToggle + S._rShieldHold=true (hold; release drops via _endShieldHold). Melee has NO right double-tap (comment: "adding a gesture there would collide with the lunge this very function fires on a first tap").
- KNOWN geometry (TouchControls.jsx): left disc at left:12/16 (portrait/landscape), bottom: sheet-h+70, base 83/98 (w/ 75/90 inner); right disc at right:RBTN.right=50, bottom: sheet-h+RBTN.bottom=70, width discW 96/108. ShieldButton: right = 50+(discW-48)/2, bottom sheet-h+12 (band under disc), 48/54px. SpecialChargePie & ElementBurstButton measure off RBTN. AbilityButtons.jsx = the ability column (need to confirm position for 'orbit near left joystick').
- INFERRED for 1(c) 'double-tap RIGHT = weapon swap': conflicts with v2.3.2446 bow/staff double-tap-hold guard (same classifier S._rTapAt/RBTN_DBL_MS) AND with melee's first-tap lunge (maybeSwordDash fires on press 1, so tap 2 would follow a lunge). If 1(b) makes the bow shield a button again (no double-tap), the right double-tap slot frees up for weapon swap. Also v2.3.2465 _atkPressAt flick-wait and v2.3.2271 lock stash (S._rTapLockWas) are tied to the same double-tap path — would move/retire with it.

## Q1 checkpoint 3 — remaining pieces
- KNOWN: special fired from BroTown.jsx doSpecialAttack (line 7486) ← rE/bE flick classifier; doSpecialAttack → playerActions.specialAttack(S) (src/game/playerActions.js:91; drops shield 'attack' at :106; mana check :145-157; aim = _aiming&&_aimAngle else lock (lockAimPoint :233)). Desktop key also calls it (mp-tutspecial). Melee arm stamps swingTimer; bow/staff arms push S.arrows (mp-solospecial header; v2.3.2464 fixed the bundled ordinary shot by spending the swing clock).
- KNOWN: ability column (AbilityButtons.jsx:118-129) sits at right: RBTN.right+discW+DISC_EDGE_GAP (or right 18/22 above the disc at bottom sheet-h+RBTN.bottom+discW+34) — ALL combat buttons are right-side. Nothing orbits the LEFT disc today except the weapon-swap preview label (lJoyPreviewRef inside left disc). A left-side Special button would be a NEW component (client-only) positioned off the left disc geometry (left:12/16, bottom sheet-h+70, 83/98px) — must not overlap lZoneRef (movement zone is the whole left half, z6) → needs pointerEvents:auto + stopPropagation like ShieldButton, and dodge = left-zone swipe (handleCanvasSwipe in lE) would need the button to swallow its own touches.
- KNOWN: Harnesses pinning Q1: mp-bowshield (15 asserts: no button on bow/staff, hold raises, drag steers, release drops, weapon cycle lowers guard), mp-rbutton (melee side, 67/68), mp-solospecial, mp-tutspecial, mp-specshield.
- INFERRED 1(b) 'bow shield defaults to nearest monster': flip = shieldButtonLive() remove the slot early-return (shieldToggle.js ~L112) → button back for bow/staff; and shieldAimAngle(S) needs a 'nearest monster' fallback because bow/staff have NO lockedTarget unless tapped (autoAcquires false; S._targetCands emptied for ranged in updateTargeting). targetCandidates(S) (targeting.js) returns nearest-first and is callable regardless of slot → INFERRED one-line fallback: `else nearest = targetCandidates(S)[0]`. The v2.3.2446 double-tap-hold would need to be disabled or coexist (it also arms _rShieldHold). mp-bowshield would be inverted. resolveCastAngle (abilities.js) reads raised shield angle for Shield Bash — check.
- INFERRED 1(d) 'Block button LEFT of the right button': ShieldButton.jsx style only: right = RBTN.right + discW + gap, bottom = sheet-h + RBTN.bottom + (discW-size)/2. BUT collisions: AbilityButtons column at right: RBTN.right+discW+DISC_EDGE_GAP, bottom sheet-h+12 (:118-121) — that IS the slot left of the disc's band; and bM's slide-to-shield latch (v2.3.2254) reads the live [data-shield] rect so it follows automatically; SpecialChargePie/ElementBurstButton measure off RBTN. Client-only.

## Q2 checkpoint 1 — bow gate/cadence/fire path
- KNOWN gate (monsterCombat.js:1450-1451): `(S.autoAttack || _engSwing) && !_flickWait && !S._shieldUp && S.rpg && _eqWpn && Date.now() - S.swingTimer >= effectiveSwingCd + _staffCdExtra`; effectiveSwingCd = max(200, floor(SWING_COOLDOWN 600 × swingCooldownMult × weaponSwingMult(slot) / atkSpdAmulet)) (:1275; bow mult 0.75 → 450ms per bow-jet-stream.md); staff +300. _flickWait = ATK_PRESS_GRACE_MS after press (v2.3.2465).
- KNOWN: NO 'sight-on-target' gate exists — bow fires whenever held & cadence elapsed, at rangedAimAngle(S, gripX, gripY) (combatHelpers.js) = ladder: lock → live _aimAngle (while _aiming) → _lastAimAngle (stick) → _facingAngle. S.arrows.push({dist 2, fromGrip, life 90, pierce/pierceLeft from bowPierceCount, _rangeMult bowRangeMult}); broadcast player_projectile (cosmetic for peers); swingTimer stamped. Server sees NOTHING at fire time; hits are client-decided in projectiles.js and reported as monster_damage.
- KNOWN server: combat.js:877 `if (nowTs - cad.n < 210) return;` per-(player,monster) cadence floor; _maxDmgForAttacker (:488) damage cap; PvE range check PVE_MELEE_RANGE 400px (comment :834-837 "at 400 px, not 250") — need to confirm whether ranged is range-checked at all (:810-840). PvP RANGE_CAP {melee:250, ranged:950, staff:950} (:1387) at impact; per-(attacker,target) cadence (:1513). No travel-time/speed validation → INFERRED: 3x arrow speed trips nothing server-side (server never simulates projectiles; mp-hitsweep header confirms "worker only CLAMPS that claim ... never re-simulates").
- KNOWN constants: BOW_RANGE_PX 675 (gameSystems.js ~2760), STAFF_RANGE_PX 675, STAFF_ORB_SPEED_PX 5, STAFF_LIFE 135; arrow 8 px/frame (480px/s at 60fps, jet-stream spec), staff 5; bow life 90 ticks (720 > 675 so plant cap governs); Longshot = bowRangeMult (cap 2.0) scales speed AND plant cap (projectiles.js:436).
- KNOWN sight stream history (mp-jetstream header): 2258 off, 2320 back while attacking, 2398 off (jet stream), 2448 back at arrow range (675×_rangeMult), while firing only; magic none.

## Q2 checkpoint 2 — projectile sim, server validation, stuck arrows
- KNOWN speed: projectiles.js:460-462 `a.dist += (a.speedPx != null ? a.speedPx : (a.isStaff ? 5 : 8 * (a._rangeMult||1))) * _pdt` (_pdt = S._dtScale, clamped ≤3). Arrow = 8 px/frame @60fps = 480 px/s; ×bowRangeMult (Longshot, cap 2.0). Plant cap :572 `a.dist > BOW_RANGE_PX * _rangeMult` or screen edge (24px margin) → planting arc. life 90 (bow) is not the limiter.
- KNOWN hit test (v2.3.2426/2433, projiles.js:684-708): CAPSULE (drawn body back/front/half from PROJ_BODY table: arrow 24/28.5/6.6, arrowSpecial 24.6/28.8/10.9) swept along the frame segment (_prevX→_renderX) vs monster circle at body centre (renderX, renderY − monsterBodyOffsetY). `_hitRE = _hitR + _hitHalf`. Segment guard: step > 200px falls back to point test (:~128 "fastest legitimate advance 8*2.0*3 = 48px"). INFERRED: 3x speed → 24 px/frame base, ×2 Longshot ×3 dtScale = 144px/frame, still < 200 guard — OK client-side, but tunnelling risk is moot with the capsule sweep. `_updateProjectileTrail` has an 80px teleport check (effectsRenderer) — 3x could trip that visual trail guard at high dtScale (INFERRED, check).
- KNOWN server: combat.js:826-843 — "Ranged/staff therefore get NO proximity gate at all"; only melee is bounded (PVE_MELEE_RANGE 400). Zone gate + 210ms per-(player,monster) cadence (:877) + special ≤3/1200ms (:873) + _maxDmgForAttacker cap. PvP (_resolvePvPAttack :1379-1513): RANGE_CAP melee 250 / ranged 950 / staff 950 measured at IMPACT report time; per-(attacker,target) cadence. NO travel-time/speed check anywhere → 3x arrow speed is CLIENT-ONLY, no anticheat coupling, no caps flag. Only coupling: the 210ms cadence floor vs the "min interval" the owner wants (bow 450ms today; anything ≥210ms is safe; a queued shot fires ≥ effectiveSwingCd anyway).
- KNOWN TRAPS §46: it is about the move-broadcast gate (`isMoving = dx||dy||_dodgeRoll`) — server range checks (_handleAbility radius 70 / reach 240) use stale ps.x/y when the client repositions without the stick (lunge). Proximity lunge covers 150–200px. Not about projectiles.
- KNOWN sight stream (effectsRenderer.js:5139-5144): `BOW_SIGHT_BEAM_ENABLED = true` (v2.3.2448), drawn `(BOW_SIGHT_BEAM_ENABLED && bowFiring) || (isMelee && ...)` — need angle source (read 5100-5200). JET_STREAM_ENABLED=false (:669).
- KNOWN 'two stuck arrows on special': projectiles.js:958-1008 pushes m._stuckArrows for EVERY non-staff arrow (no isSpecial check) — the plain-arrow stub drawn by entityRenderer (:3670 loop / 4368 STUB_FRAC noHead); AND :1180-1183 sets a.stuckIn = m for a bow special so the special arrow itself (ARROW_SPECIAL golden flame art via _placeSpecialFx) rides the monster for 4s chipping every 500ms. → both drawn = "normal + special". Fix INFERRED: skip the _stuckArrows push when a.isSpecial (client-only, projectiles.js:960).
- KNOWN stuck-arrow anchors (projectiles.js:990-1006): fireGoblin rx14/ry18/yAnchor −30; hitShapeOf==='fodder' (slime) rx9/ry7/−17; ELSE (mummy/skeleton/procedural) rx6/ry6/yAnchor 0 → planted at m.y = FEET → "arrows stick below the feet of the mummy skeleton" is this default branch: monsterBodyOffsetY says mummy 48 / skeleton 60 but the stuck anchor ignores it. Client-only fix: derive _saYAnchor from -monsterBodyOffsetY(arch) (INFERRED one-liner).
- KNOWN outline: effectsRenderer.js:531 note "black outline cause I can't see it" (ARROW_PINE) — need to read; ARROW_SPECIAL at :494.
- Harnesses: mp-hitsweep (ranged PvP hit accuracy; states client decides, worker clamps), mp-hitmatrix (100% registry rows; PvE gate = 400px melee only), mp-hitreal (real worker monsters), mp-arrowdt (speed px/s at two frame rates — a 3x change must update its expected 480 px/s), mp-arrowhead, mp-jetstream (beam length = 675×_rangeMult; pinned).

## Q3 checkpoint — hit radii
- KNOWN projectiles.js:634-668 `_hitR`: default 18; fodder/slime-sheet 27 (measured half-width 48*0.75*1.5/2); fireGoblin 26; snowman 32; mummy 40; skeleton 50; procedural = monsterProceduralRadius (min(40, liveScalePx/2)) else 48. Staff ×staffAoeMult; special ×3 (:674 → slime special = 81px!). Effective = _hitR + capsule half (arrow 6.6 → slime 33.6; special 81+10.9 = 92). Body centre for slime = renderY − 23 (monsterBodyOffsetY fodder=23).
- KNOWN Piercing: pierce=true when bowPierceCount>0, pierceLeft budget (:1187-1194); non-pierce dies on first hit (:1332); `(hit && !a.pierce)` skip. Special arrows pierce unlimited until stuckIn.
- KNOWN decision: CLIENT decides PvE hits entirely (mp-hitmatrix header: "the client's hit test is the whole story"), server only zone-gates, cadence-floors and damage-caps; no range check for ranged. INFERRED 'clearly-miss hits on slimes': the sweep is capsule (52.5px long incl. 28.5 front) vs circle 27 → an arrow passing within ~34px of the blob's centre (i.e. anywhere over its drawn body ±7px) counts; plus melee/slime body centre at −23. Also a stuck SPECIAL at ×3 radius (81px) hits slimes ~3 blob-widths away. Melee monsterMeleeHitRadius fodder 24 (gameSystems.js:6954).

## Q2 checkpoint 3 — sight stream geometry (KNOWN)
- effectsRenderer.js:5142-5144 gate `!isStaff && BOW_SIGHT_BEAM_ENABLED && bowFiring(autoAttack || _bowShotAt<BOW_SHOT_MS) && !S._shieldUp`; angle = rangedAimAngle(S, grip) (:5202, SAME ladder as the arrow: lock→_aiming/_aimAngle→_lastAimAngle→_facingAngle); origin = bow grip (S._bowGripDX/DY); length `_beamLen = BOW_RANGE_PX * bowRangeMult` (:5190); wavy polygon, segments = len/12 (:5290+). NOT clipped at monsters. For bow/staff there is NO lock unless tapped, so the line = thumb aim.
- INFERRED plan for 'fire only when line on monster / queue / stop at first monster': client-only. Gate at monsterCombat.js:1451 fire site: ray-vs-circle test using the projectile radii (_hitR table + PROJ_BODY.arrow.half) over S.monsters within _beamLen; skip stamping swingTimer when off-target (the loop retries every frame → auto-fires the first on-target frame ≥ cadence = the 'queue'); a pressed special sets a flag consumed by the same gate (specialAttack bow arm at playerActions.js:236-262 would need to defer). Beam clip = compute nearest ray hit distance and pass as lineLen (:5290). Arrow 3x = the `8` literal at projectiles.js:461 (also update mp-arrowdt's 480px/s expectation, PROJ segment guard 200px OK, trail teleport 80px check at :4053 may need raising). Nothing on the wire changes; server never sees fire time or speed.

## Q4 checkpoint — ring + stopping distance
- KNOWN melee reach: monsterCombat.js:1784-1786 `mDist = dist(P, bodyCentre) − monsterMeleeHitRadius(arch); if (mDist > _maxRange) return` where _maxRange = GS_OUTER_RADIUS 72 (inner 38 core), arc GS_FORWARD_ARC π + cleave bonus, arc widened by asin(r/d). Engaged auto-swing test (:1420-1431) `_eD = dist − monsterMeleeHitRadius ≤ GS_OUTER_RADIUS`. So a "melee perimeter ring" radius = GS_OUTER_RADIUS + monsterMeleeHitRadius(arch) (fodder 24 → 96px, skeleton 50 → 122px) around the BODY CENTRE (renderY − monsterBodyOffsetY).
- KNOWN dash: maybeSwordDash (abilities.js:586) fires on the melee press whenever a MONSTER lock exists, off 2500ms cd, shield down — at ANY distance up to DASH_MAX_REACH_PX 900 (abilities.js:116); sworddash radius 70 at arrival (data/abilities.js). There is no separate 'dash trigger radius'; the auto-lock perimeter (TARGET_PERIMETER_PX 220, hysteresis ×1.25) is what makes a press a lunge. TRAPS §46's "150–200px" is the typical auto-lock distance.
- KNOWN '!' notice: server tick field `tg` (monster's current target id); wsClient.js:815-818 stamps localM._aggroTs when tg transitions to S.myId; entityRenderer.js:7865 `aggroFlash = m._aggroTs && now − _aggroTs < NOTICE_MS (1100)` draws the mark; MONSTER_AGGRO_RANGE 120 (server/src/index.js:856; snowman 300). Existing ring drawer: effectsRenderer.js:4522-4560 (candidate ground ellipse rx = monsterMeleeHitRadius+10, ry = rx×0.38, in engageRingGfx; melee only since S._targetCands is [] for bow/staff). A light-red melee-reach ring is a client-only addition in that block keyed on m._aggroTs (fresh notice) or lock; "one at a time" = draw only for S.lockedTarget/nearest.
- KNOWN stopping distance (SERVER): index.js:1978-1985 `attackDist = sqrt(dx² + (dy·_yScale)²)`; chase while `attackDist > _atkRange` (literal 45 = MONSTER_ATTACK_RANGE, Y-scaled ellipse :1920-1935); step = m.spd px/tick (22ms tick ≈45Hz; skeleton 1.75 → ~79px/s, fodder 0.5 → 22px/s) toward the player's FEET; at ≤45 it stamps a wind-up (:2085-2106). No player↔monster separation server-side ("stays client-side", :2213-2220). CLIENT: BroTown.jsx:4784-4797 `_monBody` r = fodder 8 / snowman 13 / variants 14 / else 32 (+ player hs) — body-block + 2px/frame push-out (:4855). No client prediction of monster movement (usesClientSideMovement only for flagged variants; skeleton "server position is the source of truth"). So "stop at sword length" = server change to the 45px literal (or per-arch stop ring); optionally raise client _monBody radii. Deploy-order safe (no wire change) but anticheat coupling: melee PVE_MELEE_RANGE 400 unaffected; monster attack still lands at ≤45 — a larger stop ring must also raise the swing range or monsters stop out of their own reach (WHIFF_GRACE 1.3×45).

## Q5 checkpoint
- (a) pounce = TELEGRAPH.KITS.stalker (telegraph.js:95, kind 'pounce', windup 700, cd 6000, dmg×1.5, radius 46, leap 140; executed :328-349). Stalkers spawn ONLY in sky/Desert Winds (data.js:333: 2 stalker/2 hexer/2 volatile) and are re-skinned as MUMMY (ZONE_VARIANT_MAP.sky maps every arch → mummy). Removing = delete the kit (server) → stalker falls back to the universal basic wind-up swing only (BASIC_WINDUP, 1500ms cadence); client label gameEvents.js:851 `_maLabels.pounce` may stay (mirror-audit only requires every SERVER kit to have a client label, :563-566, not the reverse). Server-only, deploy-order safe.
- (b) skeleton anim: walk frames are DISTANCE-driven (entityRenderer.js:7213-7217 frameIdx = floor(_walkDist / walkDistPerFrame) % fc); skeleton walkDistPerFrame 3.0 (monsterVariants.js:122), 8-frame strips (skeletonSprites.js:5); at server spd 1.75px/tick ≈ 79px/s → ~26 frames/s ≈ 3.3 loops/s. Mummy: default 1.5 with 16 frames at 0.4px/tick=18px/s → 0.75 loops/s. Fix = raise skeleton.walkDistPerFrame (client-only; walkFrameMs 110 is legacy/unread).
- (c) fire goblin projectile: drawn from fireGoblinSprites fireball.png (256px) at `projectileScalePx: 24` (monsterVariants.js:208) → 24 on-screen px (effectsRenderer.js:3770). Client visual only; hit vs player for server-thrown = displayOnly (projectiles.js:1372-1378, worker resolves at impact) — legacy local slime path uses a 16px point test (:1367). Size change = one number.
- (d) orbs: 3 orbs, speedPx 5, life = round(675/5)=135 each, launchDelayMs 0/200/400 (held without aging, projectiles.js:403), so each should reach 675px (mp-orbrange asserts in a real client). Peers see STAFF_LIFE too (gameEvents.js:1352). Desert skeleton: melee only (MONSTER_ATTACK_RANGE 45, fodder lunge kit radius 50/CAST_RANGE 150 — but stalker-skinned mummies use pounce radius 46/leap 140); skeleton closes at ~79px/s. Cause of "vanish" NOT determinable from code: candidates = orb dying on FIRST hit (non-pierce, :1332) on a nearer monster/mummy transform, or the orb impacting the skeleton's 50px body at renderY−60 while the sprite is drawn 120px tall (impact FX at anchor reads as short). Needs a repro (mp-orbrange in sky zone).

## Q6 checkpoint
- (a) Debris live on ALL hit paths: melee sweep monsterCombat.js:2017 spawnHitDebris + :2200 spawnGroundDecal (chance .5); projectiles.js:753-754; peer/server monster_hit gameEvents.js:1626-1627. Not gated on _serverMonsters. BUT: (1) NONE of the five DEBRIS_BURSTS sheets exist in public/sprites/effects/ (ls shows only arrow-blast, grease, jet-stream, rocks, snowball, splash, woodchips) → every burst is the fallback: 6 tinted 32px soft dots at scale 0.3-0.7 for DEBRIS_MS = 450ms (effectsRenderer.js:863, :6466-6480) — tiny and gone in under half a second, not "~5s"; hard cap 24 bursts, 150ms per-monster dedup. (2) Ground decal = minted 64px radial blob (:923-940) × size/20 (size 4-8 → ~13-26px) at alpha ≤0.35, tinted with the material's DARK decal colour (goo #1f7a55 on grass, snow #dbeafe on snow) on layers.groundSplatter → near-invisible by construction; 8s TTL / 2s fade (:6391). INFERRED: owner isn't seeing them because they are 450ms placeholder dots + faint dark decals. "Debris lasting ~5s" = either raise DEBRIS_MS/persist particles or generate the sheets (spec manifest in combat-feel-pack.md). Client-only.
- (b) Sounds: melee → BT_AUDIO.swordHit(opts, hitMaterialOf(arch).kind) (monsterCombat.js:2138; NPC 'flesh' :2720); HIT_KEY_BY_MATERIAL goo/ember/flesh→monster-hit, bone→sword-hit3, stone→sword-hit2 (gameDisplay.js:2855-2861). Bow/staff projectile hits are NOT material-keyed: projectiles.js:875-876 `isStaff ? magicHit : play('arrow-hit')` (PvP :1306-1307). Client-only change: route through a material table (needs new arrow/orb-per-material samples or reuse swordHit keys).
- (c) Mayor damage numbers: melee sweep hits S.npcs (monsterCombat.js:2707-2748: SWING_RANGE/SWING_ARC test, npc.hp −= pDmg, pushDmgPopup(''+npcDmg,'#fff'), alive=false 10s at hp≤0). Client-only NPC hp; BroTown.jsx v2.3.2305 note explains.

## Corrections to the prompt's premises
- Right control = JOYSTICK zone (drag aims, press auto-attacks) + contextual DISC since v2.3.2258, not a pure button.
- bow cadence: BOW_SWING_MULT is 0.825 now (gameSystems.js:5557) → 495ms, not 450 (jet-stream spec is stale on that number).
- Double-tap LEFT stick = weapon swap: VERIFIED (BroTown.jsx lE ~8848-8862, 220ms release-to-release, → _desktopCycleWeapon :7881; drops shield when swapping to ranged/staff :7892).
