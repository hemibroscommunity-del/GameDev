> **Evidence appendix for `docs/BACKLOG-TRIAGE-2026-09-14.md` (v2.3.2471).** Raw checkpoint log of a read-only research pass against `main` 8f74d42 (v2.3.2470). Line numbers are as of that commit — grep the quoted identifier if they drift. `TODO` lines are the researcher's own open threads at the time of writing, not work items. KNOWN = read in code; INFERRED = the reading the code most directly supports.

# Art-bug triage scratch (read-only research, 2026-09-14T21:15Z)

## Doc reading (KNOWN, from trustworthy docs)
- gear-layer-spec.md: gear sheets `public/sprites/gear/<slot>/<item>/<pose>-<dir>.png`, 256px frames pixel-aligned to body; base dirs south/southwest/east/northeast/north; west/northwest/SOUTHEAST are horizontal mirrors generated at load (SE = mirror of SW). Gear copies body transform (`_placeGear`).
- ARMOR_SCALING_NOTES.md: body HIDDEN under full coversHead set; chain belt baked into chest sheet over waist gap (`fill_gear_gaps.py`, `bake_idle_belt.py`); jog belt: black backing removed v2.3.575. BODY_DIR_SCALE stand/jog + STAND_WIDTH/STAND_HEIGHT in entityRenderer.js.
- TRAPS §15: two placement paths — `_placeTrait` and `_placeStandaloneTrait`; audit = `grep -n 'anchor.set(' src/rendering/`.
- TRAPS §30: bare trailing arm on jog-east CLOSED v2.3.2066 via `tools/gear/draw-trailing-sleeve.mjs` (cuts sleeve along limb axis, uses body alpha). mp-shirtarm measures bare shoulder on profile; audit-only on NE/SW.
- TRAPS §70: `drawCharacterPortrait` does NOT call `artForFacing`; portrait is separate composite path from world renderer.
- TRAPS §73: attack stand-ins (`sword-<dir>-body.png`, `bow-<dir>-body.png`) baked by effectsRenderer's own `recolorBodyToCanvas` call — second bake site; 3 facings drawn by flipping a base sheet.
- tattoo-front-and-back.md: `artForFacing` (playerSkins) picks chest/back/face/headBack per facing; face decal = tattooFace; playerDecal.js owns decal placement.
- cape-and-contest.md: cape = 5 stills + meta.json like headwear; two-clone LOW/HIGH like backShield.js; §3.2 attack stand-ins hide real body -> recommended v1 = hide cape during attack poses; creator preview is a separate path (drawCharacterPortrait) -> "cape in-world not in preview is half-shipped".

## Item 5 — cape + dodge roll: cape does not disappear  => (A) one-line table edit
KNOWN: `src/rendering/systems/entityRenderer.js:1508` `const _CAPE_HIDDEN_POSES = { swing: 1, bowshot: 1, chop: 1, cook: 1, fire: 1 };`
KNOWN: the v2.3.2129 comment block (entityRenderer.js:1483-1507) says dodge/mine/fish/pickup/hit were REMOVED from the hide list on purpose ("REAL-BODY POSES — dodge, mine, fish, pickup, hit ... REMOVED, so a cape you paid for does not vanish every time you take a hit or bend down for loot"). So the cape riding the dodge roll is current designed behaviour, not a regression. Owner now wants it hidden → add `dodge: 1` back (verify the pose string; see below). _placeCape (entityRenderer.js:1661-1668) gates on `_CAPE_HIDDEN_POSES[pose]` and on `sb.visible`.
Harnesses: mp-cape.mjs / mp-capeattack.mjs / mp-capehair.mjs / mp-capekill.mjs (to read), mp-dashroll.mjs / mp-rollbake.mjs for the roll pose.

## Item 9 — south shield block: no cape  => (A) rule fix, moving-block case KNOWN; stationary case INFERRED
KNOWN: south is the ONE facing that blocks on the REAL body (entityRenderer.js:9581-9604: `if (_bf2 !== 'south' && _BOW_FACINGS.includes(_bf2)) { _bowDir=_bf2; _blockPose=true }` — south excluded because bow-south-body.png is holed through the face, v2.3.1805).
KNOWN: while MOVING with a south block, `_placeSouthBlockLegs` (entityRenderer.js:2041-2090) draws the two-band composite and sets `sb.visible = false` ("The whole body sprite would draw its own standing legs UNDER the jog band"). `_placeCape` (:1668) bails on `!sb.visible` → cape hidden. Activation: `if (_southBlock && isMoving)` (:10273). So a jogging south block always drops the cape. Fix = seat the cape from the composite's transform (bands ride sb's transform) instead of gating on sb.visible, i.e. a rule fix in _placeCape/_placeSouthBlockLegs.
INFERRED: stationary south block → sb stays visible → cape should draw; if owner saw none while standing, needs a repro (C). Note south cape is SPLIT (hood in front, panels behind body), so only the hood would be visible anyway on south; 72px shield disc sits over the chest.

## Item 14 — SW run: sword in front, should be behind  => (A) z-order table, but TWO rules to disambiguate
KNOWN: rule at entityRenderer.js:11255-11290: `inFrontInHand` = facings 0,1,2,7 (E/SE/S/NE) — SW (3) dropped v2.3.199 (bamboo behind body). `sheathed = !isInCombat`; `inFront = _heldInHand ? inFrontHeld : (sheathed ? !inFrontInHand : inFrontInHand)`.
KNOWN: `heldWeaponInFront` (:4225): bow → in front only E/S; greatsword → `inFrontBase || facingIdx === 3` (SW IN FRONT on purpose, v2.3.1787 owner: "SW SE and E need the sword layered in front of").
INFERRED: for the plain 'sword'/'staff' type OUT OF COMBAT (sheathed), SW: inFrontInHand=false → inFront = !false = TRUE → sheathed sword draws IN FRONT at SW (contradicts the v2.3.176 comment "forward facings → weapon behind"). Same inversion makes NE sheathed sword BEHIND. For greatsword the SW-in-front is an owner-requested rule (v2.3.1787) that now conflicts with this report. Owner must say which weapon (bamboo sword sheathed vs greatsword held) — either way a one-line table edit; jog not attack (attack uses sword stand-in sheets). Harnesses: mp-swordcarry, mp-southsword, mp-peersword, mp-previewweapon.

## Item 1 — copper legs jog east: bluish feet  => (C) needs measurement; likely (B) asset or body peek
KNOWN: copper = steel art + Pixi multiply tint 0xFF9E58 (materialTints.js `copper rgb [166,103,57]`, gearVariants.js `coppergreaves: art 'steelgreaves', material 'copper'`). A multiply tint cannot ADD blue: out = texel*tint, tint.b = 88/255.
KNOWN: materialTints.js header: steel sheets "99.2% neutral (<1% of opaque pixels carry any saturation)" — the <1% saturated pixels are candidates.
INFERRED: bluish feet = either (a) non-neutral (blue-shaded) pixels in `public/sprites/gear/legs/steelgreaves/jog-east.png` at the boot rows (tint can't neutralise a strongly blue texel × copper → still cool), or (b) the body's own SHOES/pants colour peeking where greaves don't cover the feet on some jog-east frames (`_maskedBodyFrame` erases body under gear dilated 6px only). Tool: `tools/qa/mp/inspect-sheets.mjs` / `tools/sprite-defringe.py` family; harness mp-coppergear pins tint+texture+visibility per pose, not colour of feet.

## Item 13 — east jog t-shirt shoulder bare AGAIN  => (B)/(C): the sleeve bake was REVERTED; art is back to bare trailing arm
KNOWN (gearSheets.js:38 GEAR_VERSION comment, version trail): v2.3.1995 sealed art → v2.3.2066 trailing sleeve baked (draw-trailing-sleeve.mjs) → v2.3.2078 reseal → v2.3.2134 renderer fix ("upper-arm capsule stamps a bare-skin body clone over the tee") → **v2.3.2140: jog-east REVERTED to the artist's sealed v2.3.1995 art** (owner complained of "an extra line drawn on the problem shoulder" = the baked hem) → v2.3.2174 de-fringe (colour only).
KNOWN (mp-shirtarm.mjs header v2.3.2133): a SIXTH owner report came AFTER the sleeve bake; the NEAR/crossing arm on frames 7-13 is bare by artist cut-out and draw-crossing-sleeve.mjs is NOT shipped (hard white bar on frames 11-13). §30's own conclusion: needs the arm's axis or 7 hand-drawn frames.
KNOWN: shirt is ONE art set (`GEAR_ART_SETS.shirt=['tshirt']`), white-base sheet tinted at render time (gearCatalog.js v2.3.748; gearSheets composeShirt tint→pattern→print). Recolours never regenerate frames from another source — every colour reads the same jog-east.png. So "only base tee baked" is moot: there is one sheet, and it currently has NO baked sleeve.
KNOWN: `loadWebpOrPng` (gearSheets.js:21) prefers .webp; a `.webp` now sits beside every PNG (mtime 2026-09-14 21:08 = generated in this sandbox; PNGs dated 2026-09-08). Need to confirm webp is build-generated from the PNG (check .gitignore/hook) — if a stale committed webp existed it would silently override the PNG.
KNOWN: local git is SHALLOW (grafted at v2.3.2358) — cannot see per-sheet history before 2026-09-08.

## Item 7 — Lil Bro shirt transparent  => (B) asset re-import; root cause is the importer's white key
KNOWN: NPC def `src/data/gameDisplay.js:3867` id 'lil_bro', strips `/sprites/npc/lil-bro-walk-<dir>.webp` (8 dirs × 4 frames) + `lil-bro-head.webp`; imported by `tools/import_npc_walk.py` (v2.3.2064).
KNOWN: `is_key()` (import_npc_walk.py:79-83): `if r > 232 and g > 232 and b > 232: return True  # the artist's white grid lines` — and `key_out()` (:88-96) applies it to EVERY pixel of the sheet, not just the border (the border is separately blanked by `clear_border`, EDGE_BLANK=5). The module docstring itself says "the shirt is neutral white". So every near-white shirt pixel was keyed transparent at import.
Fix: drop/relocate the white test (clear_border already handles grid lines) and re-run the importer on the owner's original 4x8 magenta sheet — which is NOT in the repo (tools/gear/src-art/npc has only mayor-bro files) → owner must re-supply the source. Harness mp-lilbro asserts facing/row order/height only, not shirt opacity.

## Item 10 — SW equipment preview shows no cape  => (A) code feature in characterPortrait.js (not a table edit)
KNOWN: `grep -i cape src/rendering/characterPortrait.js` → zero hits; `drawCharacterPortrait` opts (:318) = skin,pants,shoes,hair,...,shirt,dir,gear,weapon,shield — no cape. Gear layers drawn :724-726 (legs→chest→shoulders), weapon :746+. The equip screen pins the portrait to southwest (:452 "equip screen pins itself to southwest"; HeroExpanded.jsx:856 has a 'cape' eqCell). cape-and-contest.md §3.3 predicted exactly this ("a cape that renders in-world and not in the preview is a half-shipped cosmetic").
Fix: add a cape layer to the portrait (SW is a SPLIT facing: `/sprites/traits/cape/<id>/southwest.png` behind the body + `hood/southwest.png` in front, per capeSprites SPLIT_DIRS) reading the worn cape id (capeCatalog getCape / rpg._capeWorn).
Shared-machinery note: portrait `GEAR_ART_VER = '2.3.1656'` (characterPortrait.js:111) vs world `GEAR_VERSION = '2.3.2174'` (gearSheets.js:38) — the portrait can serve a CDN-cached pre-de-fringe gear sheet.

## Item 3 — south idle bow shot: left eyeball all black  => (B) asset: bow-south-body.png eye frame(s)
KNOWN: stand-in bodies are baked at effectsRenderer.js:2099 `recolorBodyToCanvas(img, skinT, pantsT, shoesT, null, _fh, null, null, a, _fw)` — signature (playerSkins.js:605) is `(img, skinT, pantsT, shoesT, shirtT, targetH, eyeT, eyeRects, art, frameW, steadyArt)`, so eyeT/eyeRects are NULL: the player's eye colour is never painted on any stand-in (EYE_MASK keyed `${pose}-${dir}` for walking sheets only, playerSkins.js:982/1189). Stand-in eyes = the sheet's own art.
KNOWN: BOW_ART_VERSION history (effectsRenderer.js:2293): "961: south bow 'load' frame (f0) camera-left eye rebuilt from the matching right eye (was an oversized white-block sclera that read as keyed-out)". So the camera-left eye of bow-south has already been hand-patched once; the black eyeball is the same eye on the same sheet (`public/sprites/player/bow-south-body.png` + `bow-south-torso.png`, frames 0-2). Fix = repaint the eye pixels in the sheet (the 961 tool — grep 'sclera' in tools/), bump BOW_ART_VERSION. Adjacent finding (TRAPS §73 shape): eye colour not plumbed to the second bake site.

## Item 2 — shirtless idle bow shot: boxed squares on torso midsection S/SE/E  => (C) needs screenshot; suspects named
KNOWN: idle bow shot draws `bow-<dir>-body.png` (cfg.bodyUrl, effectsRenderer.js:2263-2272; SE = mirrored southwest) baked via the same :2099 call with `a = artForFacing(localBodyArt(false)...)` (v2.3.2429 stamps the player's drawings on stand-ins). Bare torso ⇒ the chest canvas region is exposed.
INFERRED: "boxed squares" on a bare midsection reads like body-ink CELLS (the decal grid is 16px cells, `gridFit` in playerDecal.js) — either a chest tattoo/print/pattern stamped with opaque cell backgrounds on the stand-in, or the v2.3.2144 "pinhole fill" (BOW_ART_VERSION 962) / v2.3.2174 de-fringe leaving flat blocks. Files: effectsRenderer.js:2018-2135 (`_bakeBodyStrip`), playerDecal.js, public/sprites/player/bow-{south,southwest,east}-body.png. Harnesses: mp-standinart, mp-skinink, mp-inkframes, mp-inkoffgrid. Repro question for owner: does it happen with NO ink drawn?

## Item 4 — jog S/E bow shot: face tattoo missing on lower half of face  => (A) region rule in playerDecal.js (INFERRED mechanism)
KNOWN: `splitSkinRegions` (playerDecal.js:844-869): face = skin pixels with `y < top` where `top` = FIRST row containing any torso-band pixel in that frame; everything at/below `top` is torso or arms. Moving bow shot uses the leg-erased `bow-<dir>-torso.png` strips + jog legs (jog-legs-attack-composite.md), baked through the same second site with `artForFacing`.
INFERRED: on the bow torso strips the torso band's first row sits at jaw/chin height (raised bow arms + shoulders), so the lower half of the head is classified as NOT face and the face canvas stamps only above that row. Fix = derive the face/head boundary from the neck seed or head bbox rather than the torso band's topmost row (playerDecal.js splitSkinRegions), then verify with mp-facetat / mp-standinart / mp-inkframes.

## Item 8 — cape, east jog bow attack: cape should drape over waist, not behind  => (A) z-order rule for stand-ins
KNOWN: stand-in capes: `placeStandInCape` (entityRenderer.js:3652) + `_placeStandInCapeOn` (effectsRenderer.js:306-330) which FORCES the back half under the stand-in body: `if (backIdx > bodyIdx) parent.setChildIndex(back, bodyIdx)`; hood stays in front. Layer order constant `_STAND_IN_TRAIT_KEYS` (effectsRenderer.js:262). mp-capeattack pins `backUnderBody` via `window.__btStandInCape`. Stand-in cape has NO jog tilt/slide (comment :3625 "with its own lean already in the art").
Fix = per-(standIn,dir) exception (bow, east) keeping capeBack above the body/legs but below the weapon — one rule in `_placeStandInCapeOn`, plus updating the mp-capeattack assertion. Caveat: "draped over the waist" may also want the art's tilt; needs owner sign-off on a render.

## Item 11 — armored legs icon too small in equipped slot  => (B) icon asset padding (or a one-line UI scale)
KNOWN: `HeroExpanded.jsx:570-592` eqCell draws EVERY slot icon at `width:'80%', height:'80%', objectFit:'contain'` — no per-slot scale. Icon path = `metalIconPath('/icons/items/greaves.webp','copper')` → `greaves-copper.webp`, generated by `tools/gear/make-metal-icons.mjs` as a tint of `greaves.webp` (same canvas/padding). So the legs icon reading small = `public/icons/items/greaves.webp` has more transparent padding than `chest-plate.webp`. Fix = re-crop/normalise greaves.webp bbox and re-run make-metal-icons.mjs (copper+iron inherit), or a per-slot scale in eqCell (cheap A).

## Item 14 — UPDATE (supersedes the sheathed-inversion guess above)
KNOWN: `const isInCombat = !SHEATHED_DEFAULT_ENABLED || _combatTriggers;` (entityRenderer.js:9670) and mp-swordcarry.mjs header: "isInCombat is unconditionally true (SHEATHED_DEFAULT_ENABLED is false)". So `sheathed` is always false and the inversion branch is dead code. Live rule for SW (idx 3) jog/idle: plain sword/staff → `inFrontInHand` excludes 3 → BEHIND (already what the owner wants); GREATSWORD (`_heldInHand`) → `heldWeaponInFront` returns `inFrontBase || facingIdx === 3` → IN FRONT. The starter weapon is a copper greatsword (mp-swordcarry baseline), so the report = the v2.3.1787 greatsword exception. Fix (A): drop `|| facingIdx === 3` in `heldWeaponInFront` (entityRenderer.js:4225-4233) — this reverses an explicit owner request from v2.3.1787 ("SW SE and E need the sword layered in front of"), so confirm with the owner; SE/E stay in front via the base set. Jog/idle only — the swing uses sword-<dir> stand-in sheets with `_orderSwingWeapon`.

## Item 6 — SE jog, armored legs: gap between legs and torso on a frame or few  => (A/B) belt table regen; SE IS a mirror of SW
KNOWN: `ls public/sprites/gear/legs/steelgreaves/` has jog-{east,north,northeast,south,southwest} only → SE = mirrored jog-southwest (gear-layer-spec; gearSheets.js:9-10 "Mirroring of west/nw/se is handled by the caller"). Same for chest and belt.
KNOWN: the plate↔greaves seam is covered by the chain belt drawn as its OWN layer (entityRenderer.js:2623 `getGearFrame('belt','chainbelt','jog',dir,frameIdx)`, sheet `public/sprites/gear/belt/chainbelt/jog-southwest.png`); its anchor table `src/rendering/jogBelt.js` is generated by `tools/gen_jog_belt_table.py`, whose band height `h` = "greaves-top MEDIAN minus plate-bottom MEDIAN + 4" per DIRECTION (tool docstring). A per-direction median band cannot cover the frames whose seam is wider than median+4 → "a frame or few" with a gap.
INFERRED: which frames — measure with `node tools/qa/qa-gear-sheet.mjs --slots=legs --poses=jog` (ART-QA.md boards, all 8 facings). If SW is clean and only SE gaps, suspect mirror handling of the belt/legsFrom index instead (renderer, A). Fix path: per-frame `h` (or max instead of median) in gen_jog_belt_table.py → regenerate jogBelt.js; no sheet redraw needed unless the greaves frame itself is short.

## Item 12 — south t-shirt jog (recoloured): sliver of skin near the waist  => (B) re-seal jog-south.png; possibly the intended bare hem
KNOWN: the shirt is drawn over the bare body with NO runtime mask (seal-shirt-edges.mjs header: rejected on RAM cost); slivers are sealed INTO the sheet by `tools/gear/seal-shirt-edges.mjs` (v2.3.1873/1995), which fills only runs ≤ MAXW=2 that touch a shirt pixel and deliberately leaves neck/forearm/BELLY bare (v2.3.1480). Recolour = `composeShirt` tint→pattern→print on the same white-base sheet (gearSheets.js:141-148) — no separate recolour sheet, so a colour cannot regress the seal, but a dark tint makes a 1-2px skin run far more visible than white did.
KNOWN: `mp-southshirt.mjs` measures exactly this metric (skin px/frame along the tee's edge on jog-south; was 0.21 → 8.29 after the v2.3.2066 sleeve bake → 0.00 after v2.3.2078 reseal; jog-south itself untouched since v2.3.1995 seal + v2.3.2174 de-fringe "colour only").
Fix: run mp-southshirt to get the number; if >0 re-run seal-shirt-edges.mjs on `public/sprites/gear/shirt/tshirt/jog-south.png`, bump GEAR_VERSION AND characterPortrait SHIRT_ART_VER. If the sliver is AT the hem/waistband it may be the artist's bare-belly gap (owner call).

## Shared machinery / harness inventory
Placement paths (`grep -n 'anchor.set(' src/rendering/`, TRAPS §15): entityRenderer.js — trait on walking body `_placeHeadwear` :1177 (`anchorPx/W`), stand-in traits `_placeStandaloneTrait` :3529, stand-in cape `_placeStandaloneCapeLayer` :3617 (`standTop/W`), cape seat :1800 (`pivotY`), gear = body transform copy (`_placeGear`, sprites created 0.5/0.5 at :5423-5431, :5968-5975), weapon grip :5166/:10674/:8891 (remote), NPC :12211 (`NPC_FRAME_FEET_Y/256`). effectsRenderer.js — stand-in strips anchorY :8298/:8457/:8685/:9078/:9169, bow jog legs :8095 (waist) + gear legs :8144 (`_waist/256`), weapon grip :8922.
Bake sites (TRAPS §73): walking `getBodyFrame` (playerSkins.js:1206, eye colour + art); stand-ins `_bakeBodyStrip` effectsRenderer.js:2018/:2099 (NO eyeT/eyeRects, no steadyArt); portrait `drawCharacterPortrait` (no cape, own GEAR_ART_VER 2.3.1656 / SHIRT_ART_VER 2.3.1995 vs world GEAR_VERSION 2.3.2174).
Harnesses (tools/qa/mp/): mp-cape (cape rides body transform; off during swing), mp-capeattack (stand-in cape on, `backUnderBody===true`, hood front), mp-capehair (hood clips hair), mp-capekill (drop roll), mp-shirtarm (jog-east bare-shoulder window REPORTED not gated; gates sheet checksum = artist art 340001760), mp-jogsides (arm-clone carries shirt clone, E vs W mirror), mp-southshirt (skin px/frame along tee edge, jog-south & south block), mp-shirtkeyline (tee keyline thickness in preview), mp-facetat (face tattoo on jog frames), mp-tattoos (face/arm across wire), mp-bodyink/mp-inkback/mp-backprev (front/back canvases), mp-inkframes (ink flat across jog strip, v2.3.2470), mp-standinart (drawings on stand-ins/block), mp-standinskin (stand-in skin palette), mp-coppergear (copper tint+texture+visible per pose), mp-layer (hair over headphones, copper on swing), mp-questlegs (greaves land on legs slot), mp-swordcarry (carried greatsword scale sign), mp-southsword (south idle blade tilt), mp-peersword (remote z-order), mp-previewweapon/mp-heroview (equip-screen portrait), mp-bowside (idle bow side per facing incl. SE mirror), mp-blockarm/blockweapon/turnshield/backshield/peershield/specshield/deathshield, mp-dashroll/mp-rollbake (dodge pose), mp-lilbro (NPC facing/height only), mp-scalesheet (per-facing size). Board tool: tools/qa/qa-gear-sheet.mjs (ART-QA.md). Only image codec in sandbox: Chromium (no PIL/sharp/node_modules).
