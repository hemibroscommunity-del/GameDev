> **Evidence appendix for `docs/BACKLOG-TRIAGE-2026-09-14.md` (v2.3.2471).** Raw checkpoint log of a read-only research pass against `main` 8f74d42 (v2.3.2470). Line numbers are as of that commit — grep the quoted identifier if they drift. `TODO` lines are the researcher's own open threads at the time of writing, not work items. KNOWN = read in code; INFERRED = the reading the code most directly supports.

# loot-persist-audio triage findings (checkpoint file)

Started Mon Sep 14 21:14:47 UTC 2026

## (a)/(b) step 1 — move-broadcast gate status (KNOWN)
- src/ui/BroTown.jsx:6488 `var isMoving = dx || dy || S._dodgeRoll || S._bashDash;` — gate NOW includes the sword lunge/bash dash (v2.3.2263 fix is in). 
- The v2.3.2461/2462 dash roll (commits 32c0f04, 77f559b) is PRESENTATION ONLY: abilities.js:350 still sets `S._bashDash = {..., kind, angle}`; commit msg: "reusing S._dodgeRoll itself would have been ... a gameplay change". So the roll rides the same _bashDash flag -> still covered by the gate.
- Arrival frame: BroTown.jsx:4466-4476 `_endDash` sends an explicit move packet `{x,y,z,vx:0,vy:0}` before applyAbilityStrike (v2.3.2263).
- Knockback on the PLAYER: grep found knockback only on monsters (monsterCombat.js:742,968,2158; npc.x += ... ) and in abilities.js comments ("stun and knockback are all resolved server-side"). BroTown.jsx:5569 mentions "(knockback, a dodge roll, a server correction)" — need to check whether player knockback writes S.player.x locally w/o broadcast. TODO.

## (a)/(b) step 2 — pickup path end-to-end (KNOWN unless marked)
CLIENT (src/game/groundLoot.js):
- :50-59 anchor `_sx/_sy` = first-seen pile pos (server pos). :85 `magnetRange = 50`; :97 magnet engages when `_amPileRecipient && sDist(anchor) < 50 && lDist(visual) > 20`; pull = `(1 - lDist/50) * 3` px/frame, NOT dt-scaled (slow at edge: 0.24px/f at 46px). INFERRED: if lDist > 50 the pull goes NEGATIVE (pile pushed away) — formula quirk.
- :135 pickup trigger `lDist < 20 && loot._serverLoot && loot.lootId` -> send `{type:'loot_pickup', payload:{lootId, zone}}`; `_pickupPending` watchdog 5000ms (:140); v2.3.2327 fast re-arm 300ms only for 'out-of-range' up to 8 tries (wsClient.js:1368-1378), then falls back to 5s cadence (pending never cleared by reject handler after try 8).
- Every other reject reason (no-pile, already-claimed, not-recipient, dead, wrong-zone) => NO client feedback (console.log only, v2.3.2327 removed floater), pile stays, magnet keeps running, re-send every 5s forever. Client NEVER expires remnant piles (`:43 if (!loot.isDeathDrop && !isRemnantSkull(loot.skull) && ts>60s) return false` -> remnant piles persist forever locally).
SERVER (server/src/index.js):
- :3729 `_handleLootPickup`: rejects bad-payload, no-loot-zone, no-pile, already-claimed, not-recipient, no-ps, wrong-zone (`ps.z !== zone`), dead, disconnected, no-pet, out-of-range (`dist(ps.x/y, pile.x/y) > LOOT_PICKUP_RANGE=160`, :1052).
- :3704 `_tickLoot` expires ALL monster-kill piles at `LOOT_EXPIRY_MS=60000` (:1046) incl. remnant/skull piles; emits `loot_despawn` on the room-wide eventBuffer (tick.js:365-372). Client honours loot_despawn (gameEvents.js:227) ONLY if it receives it (must be connected + in the room at that moment).
- Anti-teleport (movement.js:245-260): `maxDist = 500*dt + 80`; reject => ps.x/y untouched BUT `ps.lastMoveAt = _now` refreshed (:296). INFERRED: a client >~91px from the server's belief that keeps streaming at 22ms cadence is stuck (each dt≈22ms => 91px budget) until it goes quiet for the 1s idle keepalive (BroTown.jsx:6531 `_idleKeepalive = now - S.lastBroadcast > 1000` -> dt=1s -> 580px budget). No correction is ever sent to the client (comment :257 "dropped silently").
- Dash arithmetic: DASH_STEP_PX=26/frame, DASH_MAX_STEP_PX=60 (abilities.js:82-83), 22ms send throttle => ≤60px per packet, under the 80px floor. So the dash itself should NOT trip the validator (INFERRED by arithmetic).
- Stick-less movers NOT in the gate: player knockback monsterCombat.js:744-745 (`P.x += cos*9`) and :971-972 (`*6`), frost sled zoneMechanics.js:137-138 (`P.x += cos(S._sled.angle)*S._sled.speed`), lifeSkillRewards.js:100/112 seat snaps. Knockback is 6-9px (harmless for the 160 radius); the SLED is a sustained stick-less move => TRAPS §46 class gap (INFERRED severity low for loot).
- Pet vacuum BroTown.jsx:5823-5858 shares `_pickupPending` and sends `viaPet:true`; server rejects `no-pet` if `ps.lifeSkills.activePet` not a number — a no-pet reject is NOT fast-re-armed (5s) and blocks the manual path's flag for 5s.
- Death pile: index.js:3623 `_spawnDeathPile` at `ps.x/ps.y`, recipients=[playerId], ownerOnlyUntil +60s, expiry +120s; pushed via room-wide `loot_drop`; client filters by pile.zone (gameEvents.js:191).

## Harness headers (KNOWN, tools/qa/mp/)
- mp-remnant.mjs (v2.3.2233): one fodder slime minted 47 remnant piles in 2.5s because in server zones the client never sets alive=false; both local kill blocks re-fired each DoT tick. Notes remnant piles are exempt from the 60s client despawn.
- mp-deathgold.mjs (v2.3.2343): death charges no gold server-side; client used to subtract 10% locally; under proto v2 unchanged fields are never re-sent so the HUD stayed wrong. Confirms "every local death path is gated on !S._serverMonsters".
- mp-lootbob.mjs (v2.3.2329) bob; mp-lootsize.mjs (v2.3.2316) sprite size — note "a pile pushed into S.groundLoot by hand is replaced wholesale by the next zone tick (piles 0 within 1.5s)".
- mp-lootzone.mjs (v2.3.2342): loot_drop rides the room-wide events buffer; client now filters pile.zone; before, a foreign death pile "fired loot_pickup requests the worker can never grant ... every request refused and re-armed".
- mp-dashhit.mjs (v2.3.2261) lunge hp check in a real spoke; mp-dashroll.mjs (v2.3.2463) pose/frame/i-frame checks.
- mp-slimeburst.mjs (v2.3.2228): burst freeze was a ReferenceError in _updateMonsters caught by pixiRenderer.js:323 try/catch that logs ONCE; corpse never reached its draw and every monster iterated after it was skipped. Blue slime: MONSTER_VARIANTS has blueSlime but VARIANT_SPRITES does not -> falls through to slime splat.
- mp-logout.mjs (v2.3.1840): logout reloads with ?noresume=1; boot check road: stored key -> ask worker -> auto-join. mp-switchbro.mjs (v2.3.2421): roster is localStorage `bt_chars` {v:1,list:[{phrase,id,name,level,at,looked}]} + cookie; "ensureChar puts the live bt_passphrase in the roster on every boot".
- tools/qa/audio-resume-check.mjs (v2.3.1593): extracts BT_AUDIO fns from src/data/gameDisplay.js (_teardownGlobalMusic, resumeFromBackground, _rebuildSources, startGlobalMusic, _ctxLive, _wakeCtx, _whenRunning, fadeIn, _ensureAudible, _ensureAnalyser, _masterIsSilent, _audioHealthCheck, _rebuildContext, _reclaimSession, noteHidden, noteVisible, reclaimIfNeeded) and runs them vs a fake AudioContext. Test 1 = "iOS stops the source WITHOUT firing onended".

## (c) step 1 (KNOWN)
- Server: index.js:2818 `_handlePlayerDeath` sets ps.dying/ps.dead BEFORE `_spawnDeathPile` (:2866); pile at ps.x/y with recipients=[playerId]. `_handleLootPickup` rejects `dead` (:3790) and `wrong-zone`; `_tickPlayerRespawn` (:2900) at +5000ms sets dead=false, z='town'.
- Client: wsClient.js:1909 `player_died` sets S._dying but the ground-loot loop (groundLoot.js) has NO `_dying`/dead guard: the death pile lands at lDist≈15 (<20) so the client immediately sends loot_pickup -> server 'dead' reject (permanent-no => 5s watchdog re-send, racing the 5000ms respawn; either 'dead' or 'wrong-zone' => no grant). INFERRED: harmless in server zones.
- respawn.js `applyLocalRespawn` does NOT clear S.groundLoot (legacy watchdog at BroTown.jsx:4297 does). Piles are dropped by the safe-zone check groundLoot.js:113 once in town.
- Client-local death path (gated `!S._serverMonsters` = town/dungeon, TRAPS #32): monsterCombat.js:1153 pushes S._deathDrops; zoneTransitions.js:954-975 re-mints LOCAL piles (no lootId/_serverLoot) on return -> local pickup path groundLoot.js:220 (no dead guard). TODO confirm whether a local pile is also pushed at death time.

## (g) KNOWN — there is NO audible pickup sound today
- src/data/gameDisplay.js:1979-1985 `beep: function beep(...) { /* v2.3.1103: DISABLED. The owner removed all procedurally-synthesised audio ("worse than nothing") ... a single no-op here silences all of them */ return; }`
- `collect()` (:2051) = two beeps => silent. `_applyLootCredit` (wsClient.js:2562, the server loot_credit ack) calls `BT_AUDIO.beep(500,...)` + `BT_AUDIO.collect()` at the end (~:2700) => both no-ops. Local path groundLoot.js:290 `BT_AUDIO.beep(500...)` also no-op.
- File SFX: `BT_AUDIO.SFX_MANIFEST` (gameDisplay.js:2579) maps names -> `/sfx/<group>/<name>.mp3`; public/audio has only music/ambient + slime/skeleton/snowman death, tree-fall, wood-chop. No coin/pickup asset exists (find public -iname '*coin*|*pickup*' -> only sprite sheets).
- Hook point for a coin sound: `_applyLootCredit` when `payload.coins > 0` (already pushes the '+N G' HUD popup), and the death-drop/remnant branches there.

## (f) step 1 (KNOWN)
- GameApp.jsx:175-262: visibilitychange -> noteHidden/noteVisible + resumeFromBackground(true); pageshow -> hard resume; focus -> soft; pointerdown/touchstart/touchend (capture) -> reclaimIfNeeded() then resume or _audioHealthCheck().
- gameDisplay.js:1829-1851: noteHidden stamps _hiddenAt; noteVisible arms _needsSessionReclaim only if away >= 2000ms; reclaimIfNeeded (in a gesture) replays silent WAV, rebuilds ctx + sources.
- _audioHealthCheck (:1885-1955): 1s watchdog; not-live ctx -> _wakeCtx, 30 asleep ticks -> rebuild; provable silence 3 ticks -> restart music (music only).
- tools/qa/audio-resume-check.mjs run 2026-09-14: "all pass" vs shipped src/data/gameDisplay.js => in-game code still matches the harness.

## SYNTHESIS (final)
### (a)/(b) — the pile runs away from the player (KNOWN formula, INFERRED sequence)
groundLoot.js:97-102: pull = `(1 - lDist/50) * 3`, applied while `sDist(anchor) < 50 && lDist(visual) > 20`, NO clamp. If lDist > 50 the pull is NEGATIVE: the pile is pushed AWAY, lDist grows, push accelerates (at 100px: -3px/f; 150px: -6px/f) — runaway while the player stays within 50px of the anchor (fighting on the spot). v2.3.1161's claim "visual pile can never stray farther than the magnet range from the position the server validates" is false in that branch. Once >160px from the anchor the pile is un-pickup-able forever: client fires at lDist<20 from the VISUAL pile, server measures ps vs ANCHOR (index.js:3800-3805) => 'out-of-range' x8 fast, then every 5s, no feedback. Reproduces (b) "coins/remnants cannot be picked up — position disagreement" (the PILE's position, not the player's).
Dash geometry: DASH_STOP_PX=46 (abilities.js:85) vs magnetRange=50 — a dash-kill leaves the pile at the edge: pull 0.24px/frame (not dt-scaled); any step/turn toward the next target flips lDist across 50 => hover/repulsion. Non-dash melee stands nearer (stronger pull, collected in frames). Matches "mainly during melee dash".
Secondary: permanent-no rejects (no-pile/already-claimed/not-recipient/dead/wrong-zone) => silent 5s loop (wsClient.js:1368-1378); pet vacuum shares `_pickupPending` (BroTown.jsx:5849-5858; PET_LOOT_RADIUS=80 from PET, server VACUUM_RANGE=240 from OWNER, `no-pet` reject holds the flag 5s and blocks the manual path); validator silent-stuck (movement.js:250-260,296). Remnants: server expires all kill piles at 60s (:3715-3717, no `expiry` on kill piles), client never expires remnants (:43) — ghost only if loot_despawn missed.
### (c)
Server-side no grant while dead (:3790). Real "same time" (INFERRED): death pile spawns at the death spot inside the pack; recovering it via loot_credit sets `S._lootFreezeUntil = now+500` (wsClient.js:2569) => player frozen 0.5s in the pack => dies => `_spawnDeathPile` re-drops the just-recovered bag. No guard either way. Client-local (town/dungeon, !S._serverMonsters) legacy path: `S._deathDrops` + local pile on zone re-entry (zoneTransitions.js:954-969), local pickup groundLoot.js:220 has no dead guard and self-credits inventory (stomped later by player_state, or not under v2 if unchanged).
### (d)
Chain: telegraph.js:595 swell event -> 1600ms -> :621 `_resolveSlimeBurst` pushes 'execute' event + `_resolveMonsterKill` (alive=false) same tick; client processes events (wsClient.js:670) before monster deltas (:718); renderer stamps `_slimeDeathStart` on first alive=false (entityRenderer.js:6594) and draws slime-death for SLIME_DEATH_MS=400 (:6546, :6737) scaled by `_peakK` (:6681). Skips: (1) a >400ms frame hitch at the kill frame (per-zone load, GC, pile sprite upload) => window elapses unseen (INFERRED); (2) monster delta culled/trimmed (spawnscale.js:315 keeps corpses only within TRIM_SAFE_PX=800) — off-screen anyway; (3) any throw in _updateMonsters aborts the rest of the loop that frame (pixiRenderer.js:323, per mp-slimeburst) — check console; (4) zone snapshot replacing S.monsters mid-death loses `_slimeDeathStart/_burstPeakFrom` (v2.3.2261 class); (5) `hasSlimeState('death')` must be loaded.
### (e)
Roster `bt_chars` (charRoster.js:41) written by ensureChar at boot (BroTown.jsx:9556, 9638), rememberChar at join (:9786) and every 30s heartbeat (:7087, needs S.myName), each `_write` mirrors to the cookie (charRoster.js:199-213). Logout (GameApp.jsx:632-636) only navigates to /?noresume=1&login=1 — writes nothing. So force-close skips no roster write. Guest tabs excluded by design (:9784). Server: rpg saved per mutation; regen coalesced REGEN_SAVE_MS=10000 (index.js:720), flushed in webSocketClose (:5007) if the close fires; char record at first join (join.js:425). No durable loss beyond <=10s regen. Player normally never SEES Continue after a force-close: bt_resume auto-rejoin within 10 min (BroTown.jsx:9868-9880) or boot check straight in (:9636).
### (f)
GameApp.jsx:190-196 visibilitychange -> noteHidden/noteVisible; :244-255 pageshow -> onResume(true) ONLY (no noteHidden/noteVisible). gameDisplay.js:1833-1836: reclaim arms only if `_hiddenAt` set and away>=2000. So a bfcache/pageshow-only return (iOS "does not always fire" visibilitychange, GameApp.jsx:246; wsClient.js:3005 "when iOS bothers to fire it") never arms the session reclaim => the v2.3.1604 detached-route mode (speaker icon, silence; every detector blind by construction, :1795-1812) has no recovery but reload. KNOWN code, INFERRED as the residual cause. Harness passes => no regression in the covered functions.
### (g)
No audible pickup sound: beep() no-op since v2.3.1103 (gameDisplay.js:1979-1985); collect() = beeps. Hook: `_applyLootCredit` (wsClient.js:2562) `payload.coins > 0`; API `BT_AUDIO.play(key,{vol})` (:3345) + SFX_MANIFEST (:2579) under public/sfx/<group>/ (no loot group yet); eager-loaded => satisfies preload law.
